# 기여 안내

기본 콘텐츠는 `data/apps/<slug>.json`만 수정합니다. 프로덕션 SQLite, `.env`, 메일 보관함, 세션 키, 테스트 계정 자료를 커밋하지 마세요.

1. 중복 도구·플랜 분리·예약 경로를 피한 slug를 정합니다. `src/lib/schema.ts`와 기존 JSON을 참고합니다.
2. 공식 제품 페이지에서 현재 이름과 제공 기능을 확인하고 URL·확인일·확인 수준을 남깁니다. 판매 여부를 확인하지 못하면 `published:false`로 둡니다.
3. 모르는 가격은 `null`입니다. 0은 확인한 무료 플랜에만 씁니다. 월 결제, 연 결제 월 환산, 일회성 가격을 구분합니다. 환율·과금 좌석·세금 포함 여부를 명시합니다.
4. 전체 제품 복제와 제한된 사용 사례를 구분하고 고유한 기능·데이터·손실·테스트 조건을 작성합니다. 현 스키마의 `editorial`은 실행 검증을 의미하지 않습니다.
5. 선행 대안의 공식 저장소와 실제 LICENSE를 확인합니다. 복합 라이선스나 source-available은 일괄 MIT로 표기하지 않습니다.
6. `pnpm validate && pnpm check && pnpm test && pnpm build`를 실행합니다. 동작을 바꿨다면 해당 브라우저 흐름도 검증합니다.
7. 변경 사유와 출처를 PR에 씁니다. 병합 후에도 실제 배포 확인 전에는 제안을 게시 완료로 표시하지 않습니다.

검증 훅을 쓰려면 `git config core.hooksPath .githooks`로 설정할 수 있습니다. GitHub Actions는 데이터·타입·단위·빌드·실제 브라우저 검증을 수행합니다. 테스트 자료는 격리된 DB에서 생성합니다.

보안 문제 제보에 세션 토큰, 실제 사용자 이메일, 비밀번호, `.env`를 포함하지 마세요. 공개 서비스의 운영 연락처는 배포자가 개인정보 처리 안내에 입력해야 합니다.
