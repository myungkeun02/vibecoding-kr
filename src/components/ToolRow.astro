---
import { categoryName, priceLabel } from '../lib/apps';
import { verdicts } from '../lib/config';
const { app: a, count = 0 } = Astro.props;
const v = verdicts[a.verdict as keyof typeof verdicts];
---

<a class="tool-row" href={'/' + a.slug}
  ><span class="tool-info"
    ><span class="tool-icon" aria-hidden="true">{a.name.slice(0, 1)}</span><span
      ><strong>{a.name}</strong><small
        >{a.nameKo !== a.name ? a.nameKo + ' · ' : ''}{categoryName(a.category)}</small
      ></span
    ></span
  ><span class="price">{priceLabel(a)}</span><span class:list={['verdict', a.verdict]}
    ><span>{v.symbol} {v.en}</span><small>{v.label}</small></span
  ><span class="vote-count" aria-label={`${count}명 대체 인증`}>↑ {count}</span></a
>
